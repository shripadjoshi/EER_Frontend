'use strict';

describe('EERApp.company module', function() {

  beforeEach(module('EERApp.company'));

  describe('company controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var companyController = $controller('CompanyController');
      expect(companyController).toBeDefined();
    }));

  });
});